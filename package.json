{
  "name": "llm-workflow-runner",
  "version": "0.1.0",
  "private": true,
  "description": "Execution core for an AI workflow automation engine. Buildable (TypeScript typechecks) but not runnable without AWS-managed infrastructure.",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit -p tsconfig.check.json"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0",

    "@aws-lambda-powertools/logger": "^2.0.0",
    "@aws-lambda-powertools/tracer": "^2.0.0",
    "@aws-lambda-powertools/metrics": "^2.0.0",

    "aws-amplify": "^6.0.0",
    "@aws-amplify/backend": "^1.0.0",

    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/client-bedrock-runtime": "^3.0.0",
    "@aws-sdk/credential-provider-node": "^3.0.0",
    "@aws-crypto/sha256-js": "^5.0.0",
    "@smithy/signature-v4": "^4.0.0",
    "@smithy/protocol-http": "^4.0.0",

    "@langchain/core": "^0.3.0",
    "@langchain/aws": "^0.1.0",
    "@langchain/openai": "^0.4.0",
    "@langchain/langgraph": "^0.2.0",

    "@pinecone-database/pinecone": "^4.0.0"
  }
}
